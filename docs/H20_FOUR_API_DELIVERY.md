# H20 Four-API Delivery Notes

The authoritative build, correctness, profiling procedure, environment, and
results are documented in the repository root [`README.md`](../README.md).

This delivery intentionally exposes four explicit MegaMoE APIs and one shared
Fable dynamic-M frontend.  Raw Nsight Systems reports are distributed outside
Git; their SHA256 manifest and structural verification are stored in
`delivery/`.
